# Contributing

Changes must keep Python, TypeScript, and Elixir behavior aligned with
`contracts/openapi.json` and the Phoenix v2 protocol documented in the root
README. Add tests for public behavior and run the package-specific checks.

Use focused pull requests. Do not commit credentials or generated build output.
Public API changes require changelog entries and must follow semantic versioning.
Contributions are licensed under Apache-2.0.
